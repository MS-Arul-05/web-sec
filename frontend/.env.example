# Supabase (publishable key is safe to expose in the browser bundle)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Optional: override the API base URL (defaults to /api in dev, /_/backend/api in prod)
# VITE_API_URL=
